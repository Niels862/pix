function fib(n: int) -> int {
    if n <= 2 {
        return 1;
    }

    return fib(n - 1) + fib(n - 2);
}

function sum(n: int) -> int {
    if n < 0 {
        return 0;
    }

    return n + sum(n - 1);
}

iprint(fib(5));
iprint(sum(100));
